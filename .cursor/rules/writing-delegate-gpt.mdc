---
description: 本文・校正・ブレストは Cursor の GPT に委譲する。親 Agent は書かない。
alwaysApply: true
---

# 文章作業は Cursor の GPT に委譲する

このリポの対話窓口は Cursor Auto。**本文・校正・講評・1問1答の質問発明は自分でしない。**

担当は `.cursor/skills/note-draft` と `.cursor/skills/kousei`。呼び出しは Cursor の Task（`generalPurpose`、モデル `gpt-5.6-luna-high`）。OpenAI の Codex CLI は使わない。

フロー: ユーザーの返答を GPT に渡して次の1問 → `brief.md` → 執筆委譲 → `/kousei`。
直してよいのは誤字・表記のみ。言い回し・断定・語尾は触らない。
詳細は `AGENTS.md` と `docs/memory/`。
