{
  "hooks": {
    "PreToolUse": [
      {
        "matcher": "Edit|Write",
        "hooks": [
          {
            "type": "command",
            "command": "jq -r '.tool_input.file_path // empty' | { read -r f; case \"$f\" in */drafts/*draft_v*.md) echo '{\"hookSpecificOutput\":{\"hookEventName\":\"PreToolUse\",\"permissionDecision\":\"deny\",\"permissionDecisionReason\":\"本人の原稿です。直接書き換えず、指摘のみ行ってください（kousei_promptのルール）。校正は/kousei スキルで。\"}}' ;; esac; }"
          }
        ]
      }
    ]
  }
}
